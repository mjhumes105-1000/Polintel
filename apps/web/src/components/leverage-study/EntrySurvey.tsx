'use client'

interface EntrySurveyProps {
  onComplete: () => void
}

export function EntrySurvey({ onComplete }: EntrySurveyProps) {
  // Survey logic will be implemented in a subsequent prompt.
  return null
}
